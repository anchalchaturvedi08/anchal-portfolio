{
  "name": "anchal-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c green,cyan \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace client",
    "dev:memory": "concurrently -n api,web -c green,cyan \"npm run dev:memory --workspace server\" \"npm run dev:web\"",
    "seed": "npm run seed --workspace server",
    "reseed": "npm run seed --workspace server -- --reset",
    "build": "npm run build --workspace client",
    "typecheck": "tsc --noEmit && npm run typecheck --workspace server && npm run typecheck --workspace client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "nodemailer": "^10.0.10"
  }
}
